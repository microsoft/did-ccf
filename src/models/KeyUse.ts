/**
 * Enumeration of key use.
 */
export enum KeyUse {
    /**
     * Key can be used for encryption.
     */
    Encryption = 'enc',

    /**
     * Key can be used for signing.
     */
    Signing = 'sig'
}
